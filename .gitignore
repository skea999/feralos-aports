# abuild outputs
sources/
packages/
pkg/
src/*/src/
src/*/pkg/
*.apk
*.tar.gz
!docs/**/*.md

# signing keys — NEVER commit private keys
*.rsa
*.rsa.pub
!feralos.pub
!src/feralos-keyring/feralos.rsa.pub

# alpine aports cache for openrc extraction (see docs/CONVERSION.md)
references/
!references/.gitkeep
cache/

# local testing
*.log
