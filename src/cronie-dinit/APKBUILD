# Contributor: FeralOS <dev@feralos.org>
# Maintainer: FeralOS <dev@feralos.org>
pkgname=cronie-dinit
pkgver=0.1.0
pkgrel=0
pkgdesc="dinit service for cronie"
url="https://github.com/skea999/feralos-aports"
arch="all"
license="ISC"
depends="dinit cronie"
makedepends=""
source="cronie"

build() { return 0; }
check() { return 0; }

package() {
	install -Dm644 "$srcdir/cronie" "$pkgdir/usr/lib/dinit.d/cronie"
	mkdir -p "$pkgdir/usr/lib/dinit.d/boot.d"
	ln -sf "../cronie" "$pkgdir/usr/lib/dinit.d/boot.d/cronie"
}

sha512sums="cc181bb0ce961ce5ad991a70d550299cec0a0a4b279d0fae33deab420a3e8225  cronie"
